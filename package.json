{
  "name": "root",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "clean": "lerna clean -y && rm -r node_modules",
    "deps:check": "lerna --concurrency 1 exec -- ncu  --loglevel verbose",
    "deps:update": "lerna --concurrency 1 exec -- ncu -u --loglevel verbose"
  },
  "workspaces": {
    "packages": [
      "workspaces/*"
    ]
  }
}
