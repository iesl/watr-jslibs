{
  "name": "root",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "clean": "lerna clean -y && rm -r node_modules",
    "deps:check": "lerna --concurrency 1 exec --stream -- ncu",
    "deps:update": "lerna --concurrency 1 exec --stream -- ncu -u",
    "tw-cli": "lerna run --scope textworks-aas --stream cli -- -- --cwd ../.."
  },
  "workspaces": {
    "packages": [
      "workspaces/*"
    ]
  }
}
