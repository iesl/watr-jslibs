version: '3.8'

volumes:
  app-volume:

services:
  rest-portal:
    image: adamchandra/watr-front
    container_name: rest-portal 
    ports:
      - "3100:3100"
    restart: always
    volumes:
      - app-volume:/usr/src/app
    networks:
      - watr-net 
